<div class="row clearfix">
            <div class="col-lg-12">
                <div class="card">
                    <div class="header">
                        <h2><strong>Liste</strong> des rendez-vous </h2>
                        
                    </div>
                    <div class="body">
                        <div class="table-responsive">
                            <table class="table table-bordered table-striped table-hover ">
                                <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>nom medecin</th>
                                        <th>nom patient</th>
                                        <th>date du rendez vous</th>
                                        <th>type du rendez vous</th>
                                        <th></th>
                                        <th></th>
                                        <th></th>

                                    </tr>
                                </thead>
                                
                                <tbody>


                                {% for r in rdv %}
                                  <tr>
                                        <td>{{r.id}}</td>
                                        <td>{{ r.med.nom }} {{r.med.prenom}}</td>
                                        <td>{{r.patient.nom}} {{r.patient.prenom}}</td>
                                        <td>{{r.daterv|date("d/m/Y à H:i")}}</td>
                                        <td>{{r.typerv}}</td>
                                        <td><a class="btn btn-raised btn-primary btn-round waves-effect" title="Modifier" href="{{ path ('update_rv',{'id':r.id}) }}">   Modifier</a></td>
                                        <td><a class="btn btn-raised btn-primary btn-round waves-effect" title="supprimer" href="{{ path ('removerv',{'id':r.id}) }}" >   supprimer </a></td>
                                        <td><a class="btn btn-raised btn-primary btn-round waves-effect" title="fiche medicale" href="{{ path ('afficheffr',{'id':r.id}) }}" >   fiche </a></td>
                                    </tr>
{% endfor %}
                                

                                


                                
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>